---
description: Solve analytical, mathematical, or logical problems using Pólya's heuristic framework. ALWAYS searches the web for related problems and techniques. Use when you need to find an unknown from given information — pure reasoning, not empirical investigation.
mode: subagent
permission:
  edit: deny
  bash: deny
  read: allow
  webfetch: allow
  ai_venice_web_search: allow
  skill: allow
  playwright_browser_navigate: allow
  playwright_browser_snapshot: allow
  playwright_browser_click: allow
  playwright_browser_type: allow
  playwright_browser_evaluate: allow
  playwright_browser_press_key: allow
  playwright_browser_take_screenshot: allow
---

Load and follow the `polya` skill. Apply the framework to the user's request. Return structured findings with sources.
